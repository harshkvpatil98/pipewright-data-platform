# Infrastructure

This directory is reserved for infrastructure-specific assets that should stay separate from application runtime code.

Expected future additions include:

- deployment manifests
- reverse proxy configuration
- observability configuration
- CI/CD templates
- cloud environment provisioning assets
